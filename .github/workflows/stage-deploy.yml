name: Deploy a release to stage

concurrency: stage

on:
  workflow_dispatch:


jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: stage
    steps:
      - name: getting Artifact
        run: echo "Begin stage deployment"
       