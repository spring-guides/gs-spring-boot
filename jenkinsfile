
pipeline {
    agent any

    environment {
        USER = 'petclinicapp'
        GROUP = 'petclinicapp'
        S3_BUCKET = 'petclinicappmy'
        BUILD_FILE_NAME = 'petclinicapp-v1.jar'
        LOCAL_FILE_PATH = "/home/${USER}/${BUILD_FILE_NAME}"
        SSH_KEY = '/home/jenkins/petclinicappkey.pem'
    }

    stages {

        stage('Build') {
            steps {
                dir('initial') {
                    sh './mvnw package'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    // Upload to S3
                    sh """
                        aws s3 cp ${WORKSPACE}/initial/target/${BUILD_FILE_NAME} s3://${S3_BUCKET}/
                    """

                    // Get running EC2 instances with tag appname=petclinic
                    def output = sh(
                        script: """
                            aws ec2 describe-instances \
                            --filters "Name=tag:appname,Values=petclinic" "Name=instance-state-name,Values=running" \
                            --query "Reservations[*].Instances[*].[InstanceId,PublicIpAddress]" \
                            --output json
                        """,
                        returnStdout: true
                    ).trim()

                    def ips = sh(
                        script: "echo '${output}' | jq -r '.[][][1]'",
                        returnStdout: true
                    ).trim().split("\n")

                    // Loop through EC2 IPs
                    for (ip in ips) {
                        echo "-----------------------------------------------------"
                        echo "Connecting to ${ip}"

                        sh """
                            ssh -o StrictHostKeyChecking=no -i ${SSH_KEY} ubuntu@${ip} << 'EOF'
                                sudo aws s3 cp s3://${S3_BUCKET}/${BUILD_FILE_NAME} ${LOCAL_FILE_PATH}
                                sudo systemctl restart petclinicapp.service
                                echo "App deployed and restarted on ${ip}"
                            EOF
                        """
                        echo "-----------------------------------------------------"
                    }
                }
            }
        }
    }
}
